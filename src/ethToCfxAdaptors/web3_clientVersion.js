const Adaptor = require('../JsonRPCAdaptor');

/**
 * eth method: web3_clientVersion
 * cfx method: cfx_clientVersion
 * 
 * inputs example:
 * 
 * outputs example:
 * 
 */
module.exports = new Adaptor();
